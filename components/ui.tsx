import { ArrowUpRight } from "lucide-react";
import { ReactNode } from "react";
export function PageHeader({ eyebrow, title, description, action }: { eyebrow?: string; title: string; description?: string; action?: ReactNode }) { return <div className="mb-8 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="mb-2 text-xs font-bold uppercase tracking-[.18em] text-[#7c8a80]">{eyebrow}</p><h1 className="text-3xl font-semibold tracking-[-.04em] text-[#19211d] sm:text-4xl">{title}</h1>{description && <p className="mt-2 max-w-xl text-sm text-[#758078]">{description}</p>}</div>{action}</div>; }
export function MetricCard({ label, value, note, accent = false }: { label: string; value: string; note: string; accent?: boolean }) { return <div className={`lift rounded-2xl border p-5 ${accent ? "border-[#c9f36d] bg-[#eaf6bd]" : "border-[#e2e9e2] bg-white"}`}><p className="text-sm text-[#758078]">{label}</p><p className="mt-3 text-3xl font-semibold tracking-[-.05em]">{value}</p><p className="mt-2 text-xs font-medium text-[#7c8a80]">{note}</p></div>; }
export function ProgressBar({ value, color = "#174b39" }: { value: number; color?: string }) { return <div className="h-2 w-full overflow-hidden rounded-full bg-[#edf1eb]"><div className="h-full rounded-full" style={{ width: `${Math.min(value, 100)}%`, backgroundColor: color }} /></div>; }
export function PrimaryButton({ children, href, type = "button", onClick }: { children: ReactNode; href?: string; type?: "button" | "submit"; onClick?: () => void }) { const classes = "inline-flex items-center justify-center gap-2 rounded-full bg-[#174b39] px-5 py-3 text-sm font-semibold text-white transition hover:bg-[#24634a] focus:outline-none focus:ring-2 focus:ring-[#c9f36d] focus:ring-offset-2"; return href ? <a href={href} className={classes}>{children}<ArrowUpRight size={16} /></a> : <button type={type} onClick={onClick} className={classes}>{children}</button>; }
export function OutlineButton({ children, href }: { children: ReactNode; href?: string }) { const classes = "inline-flex items-center justify-center gap-2 rounded-full border border-[#d9e3d8] bg-white px-5 py-3 text-sm font-semibold text-[#174b39] transition hover:border-[#174b39] focus:outline-none focus:ring-2 focus:ring-[#c9f36d]"; return href ? <a href={href} className={classes}>{children}</a> : <button className={classes}>{children}</button>; }
